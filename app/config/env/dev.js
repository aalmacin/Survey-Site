/*
        This file contains some of the most used configuration data. Dev.js is accessed separately from live.js depending on which environment the server is run on. An environment variable is done on main server application config file.
*/
module.exports = {
        dbPath: 'mongodb://localhost:27017/survey',
        secret: 'Blu3Jay5AL34STCh4mps1993'
}
